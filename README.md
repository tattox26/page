# PROYECTO NEXT.JS

# Instalaciones
apt install nodejs
apt install npm
npm install

# instalacion post
apt install nginx -y
npm run build

# Ejecucion
npm run dev o npm run start

# Docker
docker compose up -d --build